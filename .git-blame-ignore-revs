# Revisions that only reformat. Skipped by `git blame` so lines keep their real
# author. GitHub reads this file automatically; locally, opt in once with:
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# A rebase rewrites these hashes. Refresh the entry if you rebase the commit.

# style: apply ruff format
cdf9f10cf3b68c68a2d4b9562b099005767d5518
