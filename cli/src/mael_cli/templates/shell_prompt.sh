# Maelstrom worktree environment (PORT_BASE=${PORT_BASE})
# Add this to your shell configuration to show the current worktree context

export MAEL_PORT_BASE=${PORT_BASE}

# For bash: add to .bashrc
# export PS1="[mael:${PORT_BASE}] $PS1"

# For zsh: add to .zshrc
# export PROMPT="[mael:${PORT_BASE}] $PROMPT"
